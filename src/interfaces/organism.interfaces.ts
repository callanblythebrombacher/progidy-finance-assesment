import {SearchBarProps} from './molecules.interfaces.ts';
import {PickerProps} from './atom.interfaces.ts';

export interface VariableSearchBarProps {
  searchBarProps: SearchBarProps;
  pickerProps: PickerProps;
}
