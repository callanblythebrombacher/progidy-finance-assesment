import {StyleSheet} from 'react-native';

export const variableSearchBarStyles = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    position: 'relative',
    height: 90,
    paddingTop: 20,
    paddingEnd: 10,
    paddingBottom: 20,
    paddingStart: 10,
    zIndex: 2,
  },
});
